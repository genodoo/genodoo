<odoo>
  <data>2

    <!-- Tree view for cine.pelicula -->
    <record id="pelicula_view_tree" model="ir.ui.view">
      <field name="name">cine.pelicula.view.tree</field>
      <field name="model">cine.pelicula</field>
      <field name="arch" type="xml">
        <tree string="Películas">
          <field name="name"/>
          <field name="total_views"/>
          <field name="duration"/>
          <field name="link"/>          
        </tree>
      </field>
    </record>

    <!-- Form view for cine.pelicula model -->    
    <record id="pelicula_view_form" model="ir.ui.view">
      <field name="name">cine.pelicula.view.form</field>
      <field name="model">cine.pelicula</field>
      <field name="arch" type="xml">
        <form string="Película">
          <sheet>
            <group>
              <field name="image" widget="image" nolabel="1" class="oe_avatar"/>
              <field name="name"/>
              <field name="total_views"/>
              <field name="duration"/>
              <field name="link"/>
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- cine.sala tree view -->
    <record id="cinema_room_view_tree" model="ir.ui.view">
      <field name="name">cine.sala.view.tree</field>
      <field name="model">cine.sala</field>
      <field name="arch" type="xml">
        <tree string="Salas">
          <field name="room_number"/>
          <field name="capacity"/>          
        </tree>
      </field>
    </record>

    <!-- cine.sala form view -->
    <record id="cinema_room_view_form" model="ir.ui.view">
      <field name="name">Salas</field>
      <field name="model">cine.sala</field>
      <field name="arch" type="xml">
        <form string="Sala Nº">
          <sheet>
            <group>
              <field name="room_number"/>
              <field name="capacity"/>    
            </group>
          </sheet>
        </form>
      </field>
    </record>

    <!-- cinema.pelicula action window -->
    <record id="cinema_film_action" model="ir.actions.act_window">
      <field name="name">Películas</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">cine.pelicula</field>
      <field name="view_mode">tree,form</field>
      <field name="view_type">form</field>
      <field name="view_id" ref="pelicula_view_tree"/>
      <field name="help" type="html">
        <p class="oe_view_nocontent_create">
          Haz click aquí para añadir una nueva película a la cartelera
        </p>
      </field>
    </record>

    <!-- cine.sala action window -->
    <record id="cinema_room_action" model="ir.actions.act_window">
      <field name="name">Salas</field>
      <field name="type">ir.actions.act_window</field>
      <field name="res_model">cine.sala</field>
      <field name="view_mode">tree,form</field>
      <field name="view_type">form</field>
      <field name="view_id" ref="cinema_room_view_tree"/>
      <field name="help" type="html">
        <p class="oe_view_nocontent_create">
          Haz click aquí para añadir una nueva sala de tu cine
        </p>
      </field>
    </record>

    <!-- This Menu Item will appear in the Upper bar, that's why It needs NO parent or action -->
    <menuitem id="cinema_main" name="Cinema"/>

    <!-- This Menu Item Must have a parent -->
    <menuitem id="cinema_sidebar" name="Cine" parent="cinema_main"/>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="cinema_peliculas" name="Cartelera" parent="cinema_sidebar" action="cinema_film_action"/>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="_menu_act" name="Salas" parent="cinema_sidebar" action="cinema_room_action"/>

  </data>
</odoo>