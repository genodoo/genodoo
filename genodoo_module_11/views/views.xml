<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="viewtractor.list">
      <field name="name">viewtractor list</field>
      <field name="model">viewtractor.viewtractor</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="viewtractor.action_window">
      <field name="name">viewtractor window</field>
      <field name="res_model">viewtractor.viewtractor</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="viewtractor.action_server">
      <field name="name">viewtractor server</field>
      <field name="model_id" ref="model_viewtractor_viewtractor"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="viewtractor" id="viewtractor.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="viewtractor.menu_1" parent="viewtractor.menu_root"/>
    <menuitem name="Menu 2" id="viewtractor.menu_2" parent="viewtractor.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="viewtractor.menu_1_list" parent="viewtractor.menu_1"
              action="viewtractor.action_window"/>
    <menuitem name="Server to list" id="viewtractor" parent="viewtractor.menu_2"
              action="viewtractor.action_server"/>
    -->
  </data>
</odoo>